# Log Entry: 2026-08-21 - T5.2 + T5.3 Complete: Prompt Cache + Entity-Aware Dedup
**Actor:** @principal_ai / @sdet
**Type:** feature completion
**Task cards:** `tasks/T5.2.md`, `tasks/T5.3.md` -> [DONE]

## Events
1. Prompt cache live: identical rendered prompts skip the LLM entirely, keyed by SHA-256(prompt+model).
2. Dedup service live: canonical entity-aware representations make ordering/formatting irrelevant; similar snippets return the stored topology instantly WITH its real entity names (ludic labels preserved per user requirement).
3. Schema evolution: graph_cache.embedding_model added (migration-safe ALTER) after catching a design gap - cross-model vectors were being compared.
4. Integration hardening: suites are now idempotent across runs against persistent volumes (run-unique hashes/model ids) and afterAll guards handle beforeAll failures.

## State Change
Unlocks T5.4 (Langfuse observability wiring), the last G5 task. Real-model integration for both caches happens when operator adds OPENAI_API_KEY.

## Commits
- feature: add postgres-backed prompt cache keyed by prompt and model
- feature: add entity-aware embedding dedup for similar snippets