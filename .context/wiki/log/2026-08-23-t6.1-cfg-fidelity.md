# Log Entry: 2026-08-23 - T6.1 Complete: Control-Flow Fidelity (CFG Outline + Fidelity-Gated Healing)
**Actor:** @principal_ai
**Type:** feature completion (post-mission fidelity follow-up, user-reported defect)
**Task card:** `tasks/T6.1.md` -> [DONE]

## Events
1. DEFECT REPORT: a binary search snippet rendered as entry -> terminal. Diagnosis: StructuralAnalysis carried only branch COUNTS (no conditions/order/calls), the prompt gave the LLM no shape to reconstruct, validateGraph passed lazy 2-node graphs (so healing never fired), the live executor generated once WITHOUT the heal loop, and scorers rewarded any connected graph with entry+terminal.
2. analyze-code.ts now emits a per-entity CFG-lite outline (`flows`): ordered steps per top-level function with real condition source text (`loop while left <= right`), branches, returns/throws, and call steps referencing callee entities (including calls embedded in if-tests/return arguments). Optional schema field - persisted analyses and old fixtures stay valid.
3. Generation contract hardened: instructions mandate decision fan-out with labeled edges ("yes"/"no" via new edge `label` field), loop back-edge cycles, callee node links, terminal-per-completion-path; prompt serializes the outline as authoritative for order/shape. OpenAI strict-mode compatibility preserved (required-string label on the wire, defaults applied post-generation via withEdgeDefaults).
4. Fidelity is now HARD validation feeding selfHealBranch: declared conditionals without branch nodes, or declared loops without a directed cycle, are errors that trigger regeneration with injected errors; single-exit decisions and unresolved callee nodes are warnings (convergence is legal). generateWithHealing returns the normalized parse, not raw wire objects.
5. Live executor (backend/src/index.ts) switched to generateWithHealing - closes the gap where production bypassed the heal loop entirely; cache/dedup flow unchanged. 2D renderer passes edge labels through to xyflow.
6. Scorers/evals: structure scorer reserves half its score for fidelity when an analysis is supplied; eval harness gained per-case analyses plus a regression guard asserting a lazy straight-line fixture scores 0.50 (< 0.75 gate) against a branchy analysis.
7. Verification: bun test 78 pass / 0 fail; typecheck + lint exit 0; `bun run evals` PASSED including Postgres persistence (host-shell runs need TEST_DATABASE_URL=localhost override since .env's `db` host resolves only inside compose).

## State Change
Pipeline now represents real control flow: binary search yields loop-with-back-edge + labeled decision branches instead of a straight line. Lazy/degenerate topologies are rejected and healed before reaching clients.

## Commits
- (this entry) t6.1 control-flow fidelity: CFG outline extraction, fidelity-gated heal loop, fidelity-aware scorers, edge labels
