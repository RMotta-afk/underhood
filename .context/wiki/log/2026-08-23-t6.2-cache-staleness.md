# Log Entry: 2026-08-23 - T6.2 Complete: Cache Staleness Fix + Prompt-Cache Wiring
**Actor:** @principal_ai
**Type:** bugfix + feature completion (follow-up to T6.1 user report)
**Task card:** `tasks/T6.2.md` -> [DONE]

## Events
1. USER REPORT (post-T6.1 deploy): binary search still rendered as 3 unexplained cards. ROOT CAUSE: the submission hit the graph_cache EXACT-match row created by the pre-fidelity pipeline (identical code hash) — the stale 3-node topology was served without regenerating, so none of the T6.1 fidelity machinery ever ran. Secondary hazard: embedding similarity dedup could likewise serve stale topologies for any similar snippet.
2. FIX (staleness): graph_cache gained a `pipeline_version` column (migration-safe ADD COLUMN IF NOT EXISTS, existing rows default '0-legacy' which the live pipeline never reads). getCachedTopology / saveTopologyCache / findSimilarTopology are version-scoped; DedupOptions carries pipelineVersion. New `backend/src/version.ts` exports PIPELINE_VERSION='fidelity-v2' — bump on any future generation-semantics change.
3. FIX (unwired feature): SDD §6.1 prompt cache existed only as tested dead code — never called in the live path. New services/cached-generator.ts wraps the Mastra agent with withPromptCache(pool, generator): identical prompts (keyed by PIPELINE_VERSION-prefixed rendered messages + model namespace) skip the LLM; heal retries naturally miss since their injected error messages differ; completions persist per attempt. Wired into initApi alongside ensurePromptCacheTable.
4. Dev DB purged: stale graph_cache row deleted; integration-test residue (0-legacy/test-* rows, gpt-4o/groq prompt-cache entries) cleaned after verification.
5. E2E VERIFICATION (live compose stack, real model): binary search submitted via POST /analyses completed with a faithful 10-node topology — entry, boundary init, "Loop: while left <= right" branch, compute-mid process, two labeled decision branches ('yes'/'no'), range-adjust steps, two terminals; back-edges n8->n3 and n9->n3 form the loop cycle. Row stored under fidelity-v2; prompt completion cached.
6. Test-infra note: analyses.integration.test.ts concurrency suite fails if run while live app containers poll the SAME database (live workers steal queued test jobs through pg-boss). Workaround: `docker compose stop app` during host-side integration runs; suites green otherwise (12/12). Full unit suite 90 pass / 0 fail incl. new version-scoping assertions; typecheck+lint exit 0.

## State Change
Stale topologies can never be served across pipeline upgrades; prompt cache is live in production; binary search verified rendering faithfully end-to-end.

## Commits
- (this entry) t6.2 pipeline-versioned caches + prompt-cache wiring
