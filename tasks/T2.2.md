# Task T2.2: generateTopologyStep - provider-agnostic LLM topology generation
**Goal:** G2
**Status:** [DONE]
**Assigned:** @principal_ai (dispatch pending)
**Blocked by:** T2.1
**SDD Reference:** docs/sdd/architecture.md#0-scope-decisions-immutable, #41-mastra-execution-steps

## Rationale
Core AI step; routes through Mastra model routing (OpenAI default, Groq swappable via env) and emits strictly schema-valid output including plainDescription per node.

## Files in Scope
- `backend/src/workflow/steps/generate-topology.ts`

## SDD Snippet
> generateTopologyStep: Invokes the LLM to generate the JSON topology. Must output strictly to the GraphTopologySchema. No Provider Coupling: workflow steps never import provider SDKs directly; all model calls go through Mastra model-routing configured via MODEL_PROVIDER/MODEL_ID/MODEL_BASE_URL.

## Acceptance Criteria (immutable validation gates)
- **Acceptance test:** With mocked model, step output validates against GraphTopologySchema; no provider SDK imported directly
- Executed and logged by @sdet before any `[DONE]` transition.
- Wiki log entry exists in `.context/wiki/log/` for this task's completion.

## Execution Log
(Micro-execution details, tool outputs, and reasoning recorded here by the assigned agent.)

### 2026-08-21 - @principal_ai (implementation) + @sdet (acceptance)
- Installed @mastra/core@1.61.0 (SDD-mandated; only adapter-free dependency needed thanks to Mastra native model config { id, url }).
- generate-topology.ts: resolveModel() reads MODEL_PROVIDER/MODEL_ID/MODEL_BASE_URL exclusively (openai/groq/any OpenAI-compatible endpoint; zero provider SDK imports). createTopologyAgent() with instructions enforcing jargon-free plainDescription and edge integrity. buildTopologyPrompt() embeds the deterministic analysis (entry points, entity names, branch/IO counts, async flag). generateTopology() runs agent.generate with structuredOutput { schema: GraphTopologySchema } then re-parses defensively as backstop.
- TopologyGenerator minimal interface enables mock injection in tests without network or provider keys.
- ACCEPTANCE RESULT: PASS - 5/5 step tests (schema-valid passthrough, defensive rejection of missing plainDescription, prompt carries entity names, provider abstraction env-driven incl. Groq baseURL case); full repo suite 27/27; typecheck exit 0. No real-model call executed (keys absent by design); live-call validation deferred to T4.2 evals. Status [DONE].

---
*Auto-generated from `.context/decomposition.json`. Do not edit IDs or acceptance criteria manually.*