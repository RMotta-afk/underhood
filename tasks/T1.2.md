# Task T1.2: Shared Zod schemas package
**Goal:** G1
**Status:** [BLOCKED] (implementation gate: all context artifacts validated)
**Assigned:** @principal_ai (dispatch pending)
**Blocked by:** T1.1
**SDD Reference:** docs/sdd/architecture.md#3-data-models-schemas

## Rationale
Single source of truth consumed by backend workflow schemas, DB layer, and frontend rendering - including the async job contract so the frontend builds against it before G5 exists.

## Files in Scope
- `packages/types/src/index.ts`
- `packages/types/package.json`

## SDD Snippet
> NodeSchema { id, label, type: enum[entry|process|io|branch|terminal], plainDescription: string, metadata? } EdgeSchema { id, source, target, animated default true } GraphTopologySchema { nodes[], edges[], detectedPatterns[] } GraphCacheSchema { codeHash, language, topologyPayload, embedding?, createdAt } JobStatusSchema { jobId, status: enum[queued|running|completed|failed], topology?, error? }. All validation uses Zod; no manual type duplication.

## Acceptance Criteria (immutable validation gates)
- **Acceptance test:** Unit test parses sample payloads against GraphTopologySchema (required plainDescription) and JobStatusSchema; typecheck passes
- Executed and logged by @sdet before any `[DONE]` transition.
- Wiki log entry exists in `.context/wiki/log/` for this task's completion.

## Execution Log
(Micro-execution details, tool outputs, and reasoning recorded here by the assigned agent.)

---
*Auto-generated from `.context/decomposition.json`. Do not edit IDs or acceptance criteria manually.*