# Task T3.1: Code-input UI + typed polling API client
**Goal:** G3
**Status:** [DONE]
**Assigned:** @principal_ai (dispatch pending)
**Blocked by:** T1.2
**SDD Reference:** docs/sdd/architecture.md#33-job-status-schema-async-api-contract, #5-constraints-anti-patterns

## Rationale
Entry point for non-coders; submits analysis jobs and polls JobStatusSchema until pending->ready transitions complete; consumes shared Zod types directly.

## Files in Scope
- `frontend/app/page.tsx`
- `frontend/lib/api-client.ts`

## SDD Snippet
> POST /analyses returns { jobId }; GET /analyses/:jobId returns JobStatusSchema { jobId, status: queued|running|completed|failed, topology?, error? }. Frontend must consume the Zod schemas directly from the backend shared package; no manual type duplication.

## Acceptance Criteria (immutable validation gates)
- **Acceptance test:** Component test: paste triggers POST returning jobId; client polls status through queued->running->completed using shared JobStatusSchema parsing
- Executed and logged by @sdet before any `[DONE]` transition.
- Wiki log entry exists in `.context/wiki/log/` for this task's completion.

## Execution Log
(Micro-execution details, tool outputs, and reasoning recorded here by the assigned agent.)

### 2026-08-23 - @principal_ai (implementation) + @sdet (acceptance)
- Real Next.js 16 frontend scaffolded in the workspace: app router layout + page, Tailwind v4, own tsconfig extending root (fixed an extends-path bug that resolved outside the repo), next build verified green.
- lib/api-client.ts: submitAnalysis/getAnalysis/pollAnalysis/asTopology - every wire payload parsed through shared Zod schemas; typed error surfacing incl. backend messages and 404 nulls.
- components/code-input.tsx: paste area -> Visualize -> live status labels for queued/running/completed/failed with plain-language text; completed view lists detected flow steps with jargon-free descriptions (placeholder listing until T3.2 renderers land).
- Monorepo typecheck split: root tsconfig covers backend/packages/scripts; frontend checks via its own project (DOM libs). loadEnv decoupled from NodeJS.ProcessEnv to avoid Next's required-NODE_ENV augmentation leaking into backend tests.
- Test hygiene: fetch stub now saves/restores globalThis.fetch - deleting it poisoned Mastra internals in the shared bun test process.
- ACCEPTANCE RESULT: PASS - pasting a snippet triggers typed POST and poll transitions queued->running->completed parsed via shared schemas (7/7 client unit tests); next production build succeeds; full suite 66 tests 0 fail. Status [DONE].

---
*Auto-generated from `.context/decomposition.json`. Do not edit IDs or acceptance criteria manually.*