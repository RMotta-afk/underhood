# Task T1.5: Dockerfile + docker-compose (Caddy proxy + app replicas + PostgreSQL)
**Goal:** G1
**Status:** [BLOCKED] (implementation gate: all context artifacts validated)
**Assigned:** @principal_ai (dispatch pending)
**Blocked by:** T1.4
**SDD Reference:** docs/sdd/architecture.md#72-docker-topology, #52-stateless-replicas

## Rationale
Docker-first testability with replica parity: anyone runs the full stack anywhere; local Caddy proxy mirrors production LB distribution.

## Files in Scope
- `Dockerfile`
- `docker-compose.yml`
- `Caddyfile`

## SDD Snippet
> App service: multi-stage Dockerfile on Bun base; replica-ready (stateless). DB: PostgreSQL 16, named volume, healthcheck-gated. Proxy: Caddy reverse proxy fronting N app replicas. Acceptance: docker compose up --build yields end-to-end demo using only .env values.

## Acceptance Criteria (immutable validation gates)
- **Acceptance test:** docker compose config validates; app service scales to >=2 healthy replicas behind Caddy; db healthcheck-gated with named volume
- Executed and logged by @sdet before any `[DONE]` transition.
- Wiki log entry exists in `.context/wiki/log/` for this task's completion.

## Execution Log
(Micro-execution details, tool outputs, and reasoning recorded here by the assigned agent.)

---
*Auto-generated from `.context/decomposition.json`. Do not edit IDs or acceptance criteria manually.*