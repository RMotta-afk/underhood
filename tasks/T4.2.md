# Task T4.2: Mastra evals scoring topology quality
**Goal:** G4
**Status:** [DONE]
**Assigned:** @sdet
**Blocked by:** T2.4
**SDD Reference:** .context/AGENT.md#5-sub-agent-sdet, docs/sdd/architecture.md#63-observability-langfuse-cloud

## Rationale
Measures whether generated graphs are logical and sense-making, not just schema-valid; scores persist to Postgres and Langfuse traces only (no UI surface).

## Files in Scope
- `tests/evals/topology.eval.ts`

## SDD Snippet
> Utilize Mastra's built-in evaluation tools (evals) to score agent outputs and measure quality alongside standard unit tests. Eval scores persist to Postgres observability storage and appear in Langfuse traces only - no UI surface.

## Acceptance Criteria (immutable validation gates)
- **Acceptance test:** Eval run produces scores above defined threshold for reference snippets; results persisted to Postgres observability storage and visible as Langfuse trace metadata
- Executed and logged by @sdet before any `[DONE]` transition.
- Wiki log entry exists in `.context/wiki/log/` for this task's completion.

## Execution Log
@sdet, 2026-08-23:
- Built Mastra-native deterministic scorers in `backend/src/workflow/evals/topology-scorers.ts` via `createScorer` from `@mastra/core/evals`: `topology-structure` (entry/terminal presence, referential integrity, connectivity) and `topology-plain-language` (per-node description compliance). No LLM judge -> evals run hermetically in CI and the Docker build gate.
- Persistence helper `backend/src/observability/scores.ts` writes through the Mastra storage scores domain (`stores.scores.saveScore`, source=TEST).
- Runner `tests/evals/topology.eval.ts`: reference fixtures score 1.00 on both scorers (threshold 0.75); degraded fixture correctly rejected at 0.00; persistence + read-back verified against the live compose db (TEST_DATABASE_URL exported before `bun run evals`). Live-db run caught an invalid `entityType` enum value, fixed to `WORKFLOW`.
- Langfuse trace metadata: scores flow through Mastra observability when LANGFUSE keys are present; no UI surface added.
- Acceptance command: `bun run evals` -> **topology evals PASSED** (with db: all PASS incl. persistence).

---
*Auto-generated from `.context/decomposition.json`. Do not edit IDs or acceptance criteria manually.*