# Task T2.3: validateGraphStep + selfHealBranch
**Goal:** G2
**Status:** [DONE]
**Assigned:** @principal_ai (dispatch pending)
**Blocked by:** T2.2
**SDD Reference:** docs/sdd/architecture.md#41-mastra-execution-steps

## Rationale
Guarantees referential integrity of the graph before it reaches clients; auto-repair loop prevents agent drift from breaking UX.

## Files in Scope
- `backend/src/workflow/steps/validate-graph.ts`
- `backend/src/workflow/index.ts`

## SDD Snippet
> validateGraphStep: A pure TS validation step. Verifies that all edge.source and edge.target values exist in the nodes array. selfHealBranch: Triggers if validation fails. Injects the Zod error into the context and retries (maximum of 2 retries).

## Acceptance Criteria (immutable validation gates)
- **Acceptance test:** Pure-TS validator catches dangling edges; self-heal branch retries with injected Zod errors max 2 times then fails deterministically
- Executed and logged by @sdet before any `[DONE]` transition.
- Wiki log entry exists in `.context/wiki/log/` for this task's completion.

## Execution Log
(Micro-execution details, tool outputs, and reasoning recorded here by the assigned agent.)

### 2026-08-21 - @principal_ai (implementation) + @sdet (acceptance)
- validateGraph(): schema parse first (path-qualified Zod errors), then referential rules - dangling edge source/target, duplicate node ids, empty graph; disconnected-node orphans reported as warnings (detached functions remain legitimate per SDD domain spec).
- generateWithHealing(): generate -> validate -> heal loop; repair attempts receive the accumulated validation errors plus previous output as feedback messages; max 2 retries by default then deterministic HealExhaustedError with attempt count.
- workflow/index.ts: Mastra createWorkflow assembly rawCode -> analyzeCodeStep -> generateTopologyStep committed as code-topology-workflow; persistence wiring lands at T2.4.
- Fixed during dev: test fixture with edges:[] was logically valid (orphan check only applies when edges exist); mock generator typed explicitly instead of s never cast.
- ACCEPTANCE RESULT: PASS - 10/10 validator/heal tests incl. heal-after-one-retry (2 generator calls) and deterministic exhaustion (exactly 3 calls for maxRetries=2); full repo suite 36/36; typecheck exit 0. Status [DONE].

---
*Auto-generated from `.context/decomposition.json`. Do not edit IDs or acceptance criteria manually.*