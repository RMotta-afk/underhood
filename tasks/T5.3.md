# Task T5.3: Entity-aware embedding dedup service
**Goal:** G5
**Status:** [DONE]
**Assigned:** @principal_ai (dispatch pending)
**Blocked by:** T2.4
**SDD Reference:** docs/sdd/architecture.md#62-entity-aware-similar-snippet-dedup-embeddings

## Rationale
Similar snippets return cached validated topology instantly while keeping real entity names - ludic, meaningful labels preserved for non-coders.

## Files in Scope
- `backend/src/services/dedup.ts`

## SDD Snippet
> Entity-aware normalized representation: AST-derived skeleton preserving entity names (functions, variables, parameters) alongside control-flow shape, embedded via provider-routed EMBEDDING_MODEL and stored on the topology cache entry. Cosine similarity above SIMILARITY_THRESHOLD returns existing validated topology. Embedding calls subject to embedding-cache table (inputHash, model -> vector).

## Acceptance Criteria (immutable validation gates)
- **Acceptance test:** Two structurally-equivalent snippets differing only in ordering/formatting match above SIMILARITY_THRESHOLD and return the first topology; embedding calls themselves hit the embedding cache table
- Executed and logged by @sdet before any `[DONE]` transition.
- Wiki log entry exists in `.context/wiki/log/` for this task's completion.

## Execution Log
(Micro-execution details, tool outputs, and reasoning recorded here by the assigned agent.)

### 2026-08-21 - @principal_ai (implementation) + @sdet (acceptance)
- dedup.ts: canonicalRepresentation builds an order-independent entity-aware text (sorted entities + sorted structural counts + async flag) so reordered/reformatted equivalents produce identical embeddings; embedWithCache over embedding_cache table; cosineSimilarity; dedupOrRegister searches graph_cache scoped to embedding_model (new column - vectors only comparable within one model) and registers via full cache-row upsert on miss.
- Design fix during acceptance: attach-only UPDATE silently stored nothing when no cache row existed; registration now goes through saveTopologyCache with embedding + model.
- ACCEPTANCE RESULT: PASS - reordered/formatting-differing snippet dedupes at similarity >= 0.95 keeping real entity names (loadConfig/main); second identical text hits embedding cache with exactly 1 embed call. Services suite 7/7 vs live db. Status [DONE].

---
*Auto-generated from `.context/decomposition.json`. Do not edit IDs or acceptance criteria manually.*