# Task T5.1: Async API endpoints + worker pool
**Goal:** G5
**Status:** [BLOCKED] (implementation gate: all context artifacts validated)
**Assigned:** @principal_ai (dispatch pending)
**Blocked by:** T2.4
**SDD Reference:** docs/sdd/architecture.md#51-job-queue-pg-boss, #52-stateless-replicas

## Rationale
The concurrency core: fire-and-poll distribution that lets many users share one small machine without connection exhaustion.

## Files in Scope
- `backend/src/api/routes/analyses.ts`
- `backend/src/queue/worker.ts`

## SDD Snippet
> POST /analyses enqueues via pg-boss, returns { jobId }. GET /analyses/:jobId returns JobStatusSchema. In-process worker pool pulls jobs with WORKER_CONCURRENCY (default 4); durable snapshots make jobs resumable across restarts and deploys. App holds zero session state.

## Acceptance Criteria (immutable validation gates)
- **Acceptance test:** N=8 simultaneous POST /analyses all enqueue; worker pool processes up to WORKER_CONCURRENCY in parallel; all jobs reach completed with valid topology; GET returns correct statuses throughout
- Executed and logged by @sdet before any `[DONE]` transition.
- Wiki log entry exists in `.context/wiki/log/` for this task's completion.

## Execution Log
(Micro-execution details, tool outputs, and reasoning recorded here by the assigned agent.)

---
*Auto-generated from `.context/decomposition.json`. Do not edit IDs or acceptance criteria manually.*