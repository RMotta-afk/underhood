# Task T6.3: Wire topologyWorkflow Into Production
**Goal:** G6 (post-mission fidelity follow-up)
**Status:** [DONE]
**Assigned:** @principal_ai
**Blocked by:** T6.2
**SDD Reference:** docs/sdd/architecture.md#41-mastra-execution-steps

## Rationale
The Mastra `topologyWorkflow` (analyze -> generate) existed only as dead code: the live executor duplicated its logic inline, and the workflow's generate step bypassed fidelity validation/healing entirely. The workflow was never registered on the Mastra instance either, so no durable snapshots or Langfuse traces were produced.

## Files in Scope
- `backend/src/workflow/index.ts`
- `backend/src/observability/langfuse.ts`
- `backend/src/index.ts`
- `tests/workflow-mastra.test.ts` (new)

## Acceptance Criteria (immutable validation gates)
- **Acceptance test:** a createRun/start through topologyWorkflow with an injected mock generator produces a schema-valid faithful topology; a lazy first attempt triggers heal retry through the step; a live uncached API submission completes and leaves a durable run snapshot in mastra_workflow_snapshot.
- Wiki log entry exists in `.context/wiki/log/`.

## Execution Log

### 2026-08-23 - @principal_ai
- workflow/index.ts refactored into createTopologyWorkflow({ generator }) factory; generateTopologyStep now uses generateWithHealing (fidelity-gated, 2 retries) instead of single-shot generation; default exports preserved for back-compat.
- langfuse.ts createMastraWithObservability accepts an optional workflows record and registers it on the Mastra instance (run snapshots persist to shared @mastra/pg storage; step spans reach Langfuse when tracing is enabled).
- backend/src/index.ts executor now starts real runs: cache-check -> topologyWorkflow.createRun() + run.start({ inputData }) -> dedup/register on result.result; non-success status fails the job deterministically.
- tests/workflow-mastra.test.ts: standalone workflow execution with scripted mock generator proves analyze->generate composition AND heal-retry-through-step (lazy first attempt rejected, second attempt returned).
- Verified against installed @mastra/core@1.61 typings (workflows ctor param, createRun/start, WorkflowResult success/failed arms); expectSuccess helper added in tests because expect() does not narrow TS unions.
- ACCEPTANCE RESULT: PASS - unit suite green incl. 2 new workflow tests (92 pass / 0 fail with integration db); typecheck+lint exit 0; live E2E: uncached quicksort submission completed with faithful topology (loop cycle branch_2->process_2->branch_2, labeled decisions, recursion call node) and left durable snapshot row code-topology-workflow/a0a49d4c... in mastra_workflow_snapshot. Status [DONE].

---
*Post-mission follow-up task. Not part of decomposition.json.*
