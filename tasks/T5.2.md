# Task T5.2: Prompt cache layer
**Goal:** G5
**Status:** [BLOCKED] (implementation gate: all context artifacts validated)
**Assigned:** @principal_ai (dispatch pending)
**Blocked by:** T2.4
**SDD Reference:** docs/sdd/architecture.md#61-prompt-cache

## Rationale
Identical prompts skip the LLM entirely, cutting cost and latency for repeated analyses.

## Files in Scope
- `backend/src/services/prompt-cache.ts`

## SDD Snippet
> Postgres table keyed by (promptHash, modelId) -> cached completion payload. Hash = SHA-256 of fully rendered prompt string + model id. Cache hits skip the LLM entirely for identical prompts.

## Acceptance Criteria (immutable validation gates)
- **Acceptance test:** Same rendered prompt+model twice: second call hits cache table keyed by (promptHash, modelId) without invoking mocked LLM
- Executed and logged by @sdet before any `[DONE]` transition.
- Wiki log entry exists in `.context/wiki/log/` for this task's completion.

## Execution Log
(Micro-execution details, tool outputs, and reasoning recorded here by the assigned agent.)

---
*Auto-generated from `.context/decomposition.json`. Do not edit IDs or acceptance criteria manually.*