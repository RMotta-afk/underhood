# Task T6.2: Pipeline-Versioned Caches + Prompt-Cache Wiring
**Goal:** G6 (post-mission fidelity follow-up)
**Status:** [DONE]
**Assigned:** @principal_ai
**Blocked by:** T6.1
**SDD Reference:** docs/sdd/architecture.md#61-caching + #62-dedup

## Rationale
Post-T6.1 user report: binary search STILL rendered as 3 cards. Root cause: exact-match graph_cache served a stale pre-fidelity topology for the identical code hash — no generation ran at all. Additionally, the SDD §6.1 prompt cache existed as tested dead code and was never wired into the live LLM path.

## Files in Scope
- `backend/src/version.ts` (new)
- `backend/src/services/cached-generator.ts` (new)
- `backend/src/storage/postgres.ts`
- `backend/src/services/dedup.ts`
- `backend/src/index.ts`

## Acceptance Criteria (immutable validation gates)
- **Acceptance test:** a pre-fidelity graph_cache row is never served after upgrade (version scoping); identical prompt hits the prompt cache instead of the LLM; live E2E binary search submission completes with branch nodes + loop cycle.
- Wiki log entry exists in `.context/wiki/log/`.

## Execution Log

### 2026-08-23 - @principal_ai
- graph_cache.pipeline_version column added (default '0-legacy' for pre-existing rows); all reads/writes version-scoped; PIPELINE_VERSION='fidelity-v2' exported from backend/src/version.ts and threaded through executor + DedupOptions.
- withPromptCache(pool, generator) decorator wires prompt-cache into generation; version prefix in key invalidates on pipeline bumps; heal-retry prompts differ so they always miss naturally.
- Purged stale dev-db row + integration residue; rebuilt image; restarted stack.
- ACCEPTANCE RESULT: PASS - live E2E binary search -> 10-node faithful topology (loop back-edges, labeled yes/no decisions, two terminals), cached under fidelity-v2; unit suite 90/0; typecheck/lint exit 0. Known infra caveat: pause app containers when running host-side pg-boss integration suites against the same db (live workers steal queued jobs). Status [DONE].

---
*Post-mission follow-up task. Not part of decomposition.json.*
