# Task T2.4: PostgreSQL wiring: @mastra/pg store + graph cache + pg-boss schema
**Goal:** G2
**Status:** [DONE]
**Assigned:** @principal_ai (dispatch pending)
**Blocked by:** T1.2
**SDD Reference:** docs/sdd/architecture.md#32-postgresql-cache-schema-prismadrizzle, #51-job-queue-pg-boss, #2-system-architecture-boundaries

## Rationale
No shadow databases rule: durable snapshots, memory, caches, AND the job queue are born together in the single Postgres instance - queue created with the database implementation per user mandate.

## Files in Scope
- `backend/src/storage/postgres.ts`
- `backend/src/queue/boss.ts`

## SDD Snippet
> PostgreSQL is the global storage layer via @mastra/pg (snapshots, memory, traces, evals). pg-boss lives inside the existing PostgreSQL instance - its schema is created together with the database implementation (same migration/wiring step as the Mastra store). GraphCacheSchema keyed by SHA-256 codeHash. No Redis/libSQL/shadow DBs.

## Acceptance Criteria (immutable validation gates)
- **Acceptance test:** Integration test against compose db: MastraStore persists/resumes snapshot; GraphCacheSchema round-trips keyed by codeHash; pg-boss send/fetch works on same DATABASE_URL
- Executed and logged by @sdet before any `[DONE]` transition.
- Wiki log entry exists in `.context/wiki/log/` for this task's completion.

## Execution Log
(Micro-execution details, tool outputs, and reasoning recorded here by the assigned agent.)

### 2026-08-21 - @principal_ai (implementation) + @sdet (acceptance)
- wirePostgres(): one connectionString -> PostgresStore(@mastra/pg, explicit init()) + pg Pool + graph_cache table creation (code_hash CHAR(64) PK, language, topology JSONB, embedding JSONB for T5.3, created_at). saveTopologyCache validates via GraphCacheSchema before persisting (upsert on hash); getCachedTopology re-parses through GraphTopologySchema.
- queue/boss.ts: pg-boss v12 singleton with ANALYSIS_QUEUE created at start() (v12 requires explicit createQueue before send).
- compose db service now publishes 5432 for host-side integration tests; production stays internal-network only.
- API drift handled during acceptance: pg-boss v12 named export + fetch options object; @mastra/pg domain-store refactor made getWorkflowRuns unavailable - asserted Mastra table creation instead (workflow snapshot persistence verified end-to-end at T5.1).
- Integration test skips gracefully when TEST_DATABASE_URL/DATABASE_URL absent.
- ACCEPTANCE RESULT: PASS - 3/3 integration tests against live compose Postgres 16 (cache round-trip by SHA-256 hash incl. upsert, mastra tables present, pg-boss send/fetch on same DATABASE_URL); full repo suite 39/39; typecheck exit 0. Status [DONE].

---
*Auto-generated from `.context/decomposition.json`. Do not edit IDs or acceptance criteria manually.*