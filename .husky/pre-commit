# Underhood pre-commit gate (hardened):
# 1. Secrets firewall scan on staged additions (AGENT.md mandate)
# 2. lint-staged -> ESLint strictTypeChecked on staged .ts files
# 3. Full-repo strict typecheck + test suite
bun run check:secrets && bunx lint-staged && bun run typecheck && bun test
