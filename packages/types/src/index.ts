// @underhood/types — single source of truth for all shared Zod schemas.
// Populated by T1.2: GraphTopologySchema, NodeSchema, EdgeSchema,
// GraphCacheSchema, JobStatusSchema.
export const PACKAGE_VERSION = "0.1.0" as const;
